#include <stdio.h>
#define YES 1
#define NO 0

int main(){

    int c, is_word, not_word;

    while((c = getchar()) != EOF){
        if (c == '\n' || c == ' ' || c == '\t'){
            putchar('\n');
        }
        else {
            putchar(c);
        }
        }
    }
